"use client";
import { useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { PlusIcon } from "@heroicons/react/24/outline";
import { useSitePreferences } from "@/context/SitePreferences";
import { useSiteContent } from "@/context/SiteContent";
export default function ExperienceSection(){const{language}=useSitePreferences();const{content}=useSiteContent();const[active,setActive]=useState(0);const t=content[language].experience;return <section id="deneyim" className="section-wrap section-space experience-section"><div className="experience-head"><p className="mini-label">{t.label}</p><h2>{t.title}</h2></div><div className="timeline">{t.items.map((j,i)=>{const isOpen=active===i;return <motion.article key={`${j.company}-${i}`} initial={{opacity:0,y:12}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className={`timeline-item ${isOpen?"is-open":""}`}><button type="button" className="timeline-row" onClick={()=>setActive(isOpen?-1:i)} aria-expanded={isOpen}><time>{j.period}</time><div><p className="role">{j.role}</p><h3>{j.company}</h3></div><div className="timeline-skills">{j.tags.slice(0,2).join(" · ")}</div><PlusIcon className="timeline-toggle"/></button><AnimatePresence initial={false}>{isOpen&&<motion.div className="timeline-detail" initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}}><div><p>{j.text}</p><div className="tag-row">{j.tags.map(tag=><span key={tag}>{tag}</span>)}</div></div></motion.div>}</AnimatePresence></motion.article>})}</div></section>}
